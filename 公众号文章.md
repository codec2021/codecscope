# 一个浏览器里就能用的视频码流分析工具：CodecScope

> 无需安装、无需后端，拖入 H.264/H.265/H.266 码流或 MP4，浏览器里直接看透每一比特。

## 写在前面

做音视频开发，最头疼的事之一就是**看码流**。

调试编码器、排查花屏、分析 GOP 结构、核对 SPS/PPS 参数……这些场景下，我们往往需要在各种命令行工具、十六进制编辑器之间来回切换：用 ffprobe 看个概览，用 xxd 看字节，再手动对照规范去数 bit 位。

有没有一个工具，能**把码流的结构直观地展示出来**，点一下 NAL 就能看到完整的语法元素解析，还能顺便看画面？

这就是 **CodecScope** —— 一个纯浏览器运行的视频码流分析工具。

## 它是什么

CodecScope 是一个开源的 H.264/H.265/H.266 码流分析工具，核心解析器用 C++ 编写、通过 Emscripten 编译成 WebAssembly，前端用原生 HTML/CSS/JS，托管在 GitHub Pages 上。

**零安装、零后端**：浏览器打开就能用，隐私安全（文件完全在本地解析，不上传服务器）。

> 在线地址：https://codec2021.github.io/codecscope/
> 源码：https://github.com/codec2021/codecscope

## 核心能力

### 1. 三码流自动识别

H.264 (AVC)、H.265 (HEVC)、H.266 (VVC) 三种标准全部支持，码流类型**自动识别**，拖进去就能解析，不用手动选。

### 2. NAL 单元列表 + 语法树

解析后左侧是完整的 NAL 单元列表（虚拟滚动，几万 NAL 的大文件也能流畅），点任意一个 NAL，右侧立刻展开它的**逐位语法元素树**：

- SPS / PPS / VPS 里的每一个字段
- Slice Header 的参考帧、POC、QP 等
- 同时给出 Hex 十六进制 + ASCII 视图

再也不用手动数 bit 了。

### 3. 帧结构时间轴（GOP 可视化）

I/P/B 三种 slice 用红/蓝/绿三色画成时间轴，GOP 结构一目了然：

- 帧号、POC 标记
- 帧与帧之间的物理分隔
- 支持缩放、逐帧步进
- **多 slice 码流正确分帧**（一帧 8 个 slice 不会被拆成 8 帧）

### 4. 视频播放

基于 WebCodecs，点击时间轴上的帧就能预览画面，还能连续播放：

- 按码流**真实帧率**播放（从 VUI 里解析出的 FPS）
- **解码顺序 / 显示顺序**一键切换，正确处理 B 帧重排
- 直接拖入 MP4/MOV，自动解封装后也能播

### 5. MediaInfo 式容器信息

拖入 MP4/MOV，自动解析完整容器结构，树状展示：

- **General**：格式、时长、总体码率、编码日期
- **Video**：Codec ID、分辨率、帧率、位深、色度抽样、流大小
- **Audio**：PCM/AAC、声道数、采样率、位深、码率
- **Other**：meta 轨道（如 Sony 相机的 rtmd 元数据）

基本对齐 MediaInfo 的输出，无需再单独开一个工具。

### 6. HEIC/HEIF 图片

iPhone 拍的 HEIC 也能直接拖进来：自动识别、解析分辨率 / 位深 / 色度 / Profile，展示图像 IDR 帧的语法结构，还能预览画面。

### 7. 实用统计

- NALUs / Slices 数量、I/P/B 占比
- Profile、Level、Tier、分辨率、真实帧率
- HDR 信息（色彩原色、传输特性、CLL、Mastering Display）
- 警告面板：数值越界、引用结构缺失、Profile 一致性检查

## 一个典型的排查场景

假设你拿到一段编码后的 H.265 码流，发现播放器花屏：

1. 拖进 CodecScope，先看统计面板：分辨率、Profile、Level、帧率对不对
2. 点开第一个 SPS，核对 `bit_depth_luma_minus8`、`chroma_format_idc` 等关键参数
3. 看时间轴，确认 GOP 结构是否合理（I 帧间隔、B 帧数量）
4. 点几个 B 帧的 Slice Header，检查参考帧列表是否正确
5. 切到预览 Tab，看画面在哪一帧开始花

整个排查过程**一个网页全搞定**。

## 技术实现小记

- **解析核心**：复用开源项目 hevcparser 的纯 C++ 解析器，并为 H.264、H.266 各实现了一套新解析器
- **WebAssembly**：C++ 通过 Emscripten 编译为 WASM，前端 JSON 序列化交互
- **MP4 解封装**：纯 JS 实现 ISO BMFF 解析，提取 hvcC/avcC description
- **解码**：WebCodecs API，`hvcC` description + length-prefixed NAL 对齐浏览器硬解路径
- **移动端**：响应式布局，窄屏自动切换底部导航

## 开源与许可

项目基于 GPL v2 开源，作者 codec2021。

- GitHub：https://github.com/codec2021/codecscope
- 在线体验：https://codec2021.github.io/codecscope/

欢迎 Star、提 Issue、提 PR。

---

*如果你也在做音视频开发，收藏这个工具，下次调试码流会快很多。*
